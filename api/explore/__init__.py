"""Explore module: read-only exposure lookup (FR-13 to FR-16).

Bounded 2-hop traversal from a party, loan, or property anchor, with the
degree guard and signal flag references. Never writes, never triggers rules.
"""
